@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Poppins:wght@300;400;600&display=swap');

.modern-loading-screen {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  z-index: 9999;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background-color: #050505;
  overflow: hidden;
  font-family: 'Poppins', sans-serif;
  animation: fadeIn 1s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* Background & Overlay */
.forest-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: url('/src/assets/image.png') no-repeat center center/cover;
  filter: blur(15px) brightness(0.4);
  z-index: 1;
  transform: scale(1.1);
}

.glass-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle at center, rgba(0, 0, 0, 0.2) 0%, rgba(0, 0, 0, 0.8) 100%);
  backdrop-filter: blur(5px);
  z-index: 2;
}

/* Content Container */
.loading-content {
  position: relative;
  z-index: 3;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 50px;
}

/* 3D Flipping Flashcard Loader */
.flipping-card-loader {
  width: 200px;
  height: 280px;
  perspective: 1000px;
}

.loader-card-inner {
  position: relative;
  width: 100%;
  height: 100%;
  text-align: center;
  transition: transform 0.8s;
  transform-style: preserve-3d;
  animation: infiniteFlip 3s cubic-bezier(0.4, 0, 0.2, 1) infinite;
}

@keyframes infiniteFlip {
  0% { transform: rotateY(0); }
  50% { transform: rotateY(180deg); }
  100% { transform: rotateY(360deg); }
}

.loader-card-front, .loader-card-back {
  position: absolute;
  width: 100%;
  height: 100%;
  backface-visibility: hidden;
  border-radius: 20px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
}

.loader-card-front {
  background: rgba(20, 20, 20, 0.9);
  backdrop-filter: blur(10px);
  color: #facc15; /* Yellow Accent */
  border: 1px solid rgba(250, 204, 21, 0.3);
  box-shadow: 0 0 20px rgba(250, 204, 21, 0.1), inset 0 0 10px rgba(250, 204, 21, 0.05);
}

.loader-card-back {
  background: rgba(10, 10, 10, 0.9);
  backdrop-filter: blur(10px);
  color: #4ade80; /* Neon Green Accent */
  transform: rotateY(180deg);
  border: 1px solid rgba(74, 222, 128, 0.3);
  box-shadow: 0 0 20px rgba(74, 222, 128, 0.1), inset 0 0 10px rgba(74, 222, 128, 0.05);
}

.loader-icon {
  font-family: 'Orbitron', sans-serif;
  font-size: 3rem;
  margin-bottom: 15px;
  text-shadow: 0 0 15px rgba(250, 204, 21, 0.5);
}

.loader-card-front h2 {
  font-family: 'Orbitron', sans-serif;
  font-size: 1.2rem;
  letter-spacing: 4px;
  font-weight: 700;
}

.loader-loading-icon {
  animation: spin 2s linear infinite;
  margin-bottom: 15px;
  color: #4ade80;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.loader-card-back h3 {
  font-family: 'Orbitron', sans-serif;
  font-size: 1rem;
  letter-spacing: 2px;
}

.dots {
  display: inline-block;
  width: 20px;
  text-align: left;
}

/* Progress Section */
.progress-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 15px;
  width: 400px;
}

.loading-status-text {
  font-family: 'Orbitron', sans-serif;
  color: #ffffff;
  font-size: 0.8rem;
  letter-spacing: 2px;
  opacity: 0.8;
}

.modern-progress-container {
  width: 100%;
  height: 6px;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 10px;
  overflow: hidden;
  border: 1px solid rgba(255, 255, 255, 0.05);
}

.modern-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #facc15, #4ade80);
  border-radius: 10px;
  position: relative;
  transition: width 0.1s linear;
}

.progress-glow {
  position: absolute;
  right: 0;
  top: 0;
  height: 100%;
  width: 20px;
  background: #fff;
  filter: blur(10px);
  box-shadow: 0 0 15px #4ade80;
}

.percentage-text {
  font-family: 'Orbitron', sans-serif;
  color: #4ade80;
  font-size: 0.9rem;
  font-weight: 700;
}

/* Footer Tip */
.loading-footer {
  position: absolute;
  bottom: 40px;
  z-index: 3;
  color: rgba(255, 255, 255, 0.4);
  font-size: 0.8rem;
  letter-spacing: 1px;
}

/* Exit Animation */
.modern-loading-screen.fade-out {
  animation: fadeOut 0.8s forwards;
}

@keyframes fadeOut {
  to { opacity: 0; visibility: hidden; }
}

@media (max-width: 500px) {
  .progress-section {
    width: 90%;
  }
}
